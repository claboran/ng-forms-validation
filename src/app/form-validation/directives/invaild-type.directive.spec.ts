import { InvaildTypeDirective } from './invaild-type.directive';

describe('InvaildTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new InvaildTypeDirective();
    expect(directive).toBeTruthy();
  });
});
